# =============================================================================
# VULNERABLE PYTHON DEPENDENCIES — for security scanner testing
# Covers all pip version specifier formats and known CVEs
# =============================================================================

# =============================================================================
# PINNED EXACT VERSIONS (==)
# =============================================================================

# Web frameworks — known vulnerable versions
Django==2.0.0                       # CVE-2019-14232 — ReDoS in django.utils.text
Flask==0.12.0                       # CVE-2018-1000656 — DoS via JSON
tornado==4.5.0                      # CVE-2023-28370 — open redirect
bottle==0.12.13                     # CVE-2020-28473 — CRLF injection
aiohttp==3.7.3                      # CVE-2021-21330 — open redirect
Werkzeug==0.16.0                    # CVE-2023-25577 — high resource usage DoS
gunicorn==19.4.0                    # CVE-2018-1000164 — HTTP response splitting
twisted==18.9.0                     # CVE-2022-21712 — cookie/header leak via redirect
falcon==2.0.0                       # Known vulnerable version
sanic==19.12.0                      # Known vulnerable version

# Serialization / parsing
PyYAML==5.1                         # CVE-2020-1747 — arbitrary code execution via FullLoader
lxml==4.6.2                         # CVE-2021-28957 — XSS via clean_html
defusedxml==0.5.0                   # Outdated — missing later fixes
ujson==1.35                         # CVE-2021-45958 — double free / heap overflow
msgpack==0.6.0                      # Known vulnerable version

# Cryptography and auth
cryptography==2.3                   # CVE-2023-23931 — memory corruption
PyJWT==1.5.0                        # CVE-2022-29217 — algorithm confusion
python-jose==3.0.0                  # CVE-2024-33663 — JWE decryption key confusion
itsdangerous==0.24                  # Known vulnerable version
paramiko==2.4.0                     # CVE-2023-48795 — Terrapin attack
pyOpenSSL==17.5.0                   # CVE-2018-1000807 — use-after-free
pycryptodome==3.9.0                 # Known vulnerable version

# HTTP / networking
requests==2.19.0                    # CVE-2023-32681 — leaking Proxy-Authorization headers
urllib3==1.24.1                     # CVE-2021-33503 — ReDoS
httplib2==0.18.0                    # CVE-2021-21240 — ReDoS
httpx==0.22.0                       # Known vulnerable version
certifi==2022.9.24                  # CVE-2023-37920 — removal of e-Tugra root cert

# Database
SQLAlchemy==1.2.0                   # Known vulnerable version
psycopg2==2.7.0                     # Known vulnerable version
mysqlclient==1.3.0                  # Known vulnerable version
pymongo==3.4.0                      # Known vulnerable version
redis==3.0.0                        # Known vulnerable version

# Data science / ML
numpy==1.16.0                       # CVE-2021-33430 — buffer overflow
scipy==1.1.0                        # CVE-2023-25399 — DoS via refcount overflow
pandas==0.23.0                      # Known vulnerable version
Pillow==6.2.0                       # CVE-2021-25287, CVE-2021-25288 — out-of-bounds read
matplotlib==3.0.0                   # Known vulnerable version
scikit-learn==0.19.0                # Known vulnerable version
tensorflow==2.5.0                   # CVE-2021-37678 — arbitrary code execution
torch==1.8.0                        # Known vulnerable version

# Cloud / infrastructure
boto3==1.9.0                        # Known vulnerable version
google-cloud-storage==1.18.0       # Known vulnerable version
azure-storage-blob==2.1.0          # Known vulnerable version
ansible==2.9.0                      # CVE-2021-3583 — template injection
docker==4.1.0                       # Known vulnerable version

# Task queues / messaging
celery==4.2.0                       # CVE-2021-23727 — command injection
kombu==4.2.0                        # Known vulnerable version
flower==0.9.2                       # Known vulnerable version

# =============================================================================
# COMPATIBLE RELEASE (~=) — allows patch-level updates
# =============================================================================
Jinja2~=~=3.1.6                      # CVE-2024-22195 — XSS
Mako~=1.0.0                         # CVE-2022-40023 — ReDoS
MarkupSafe~=1.0                     # Known vulnerable version
Pygments~=2.4.0                     # CVE-2021-20270 — infinite loop
beautifulsoup4~=4.6.0               # Known vulnerable version
html5lib~=1.0                       # Known vulnerable version
python-dateutil~=2.6.0              # Known vulnerable version

# =============================================================================
# MINIMUM VERSION (>=) — no upper bound
# =============================================================================
setuptools>=>=82.0.1                  # CVE-2022-40897 — ReDoS
pip>=19.0                           # CVE-2021-3572 — URL injection
wheel>=0.30.0                       # Known vulnerable version

# =============================================================================
# RANGE CONSTRAINTS (>=, <) — bounded ranges
# =============================================================================
pydantic>=1.0,<1.9                  # Known vulnerable range
marshmallow>=2.0,<3.0               # Known vulnerable range
click>=6.0,<7.0                     # Known vulnerable range
rich>=9.0,<10.0                     # Known vulnerable range

# =============================================================================
# EXCLUSION (!=) — exclude specific broken versions
# =============================================================================
six>=1.10,!=1.13.0                  # 1.13.0 had a regression
idna>=2.5,!=2.8                     # 2.8 had compatibility issues

# =============================================================================
# WILDCARD (*) — match any at a given level
# =============================================================================
colorama==0.3.*                     # Known vulnerable range
docutils==0.14.*                    # Known vulnerable range

# =============================================================================
# ARBITRARY EQUALITY (===) — exact string match, rarely used
# =============================================================================
some-internal-package===1.0.0.dev1  # Pre-release arbitrary match

# =============================================================================
# NO VERSION SPECIFIER — pulls latest at install time (unpinned)
# =============================================================================
nose                                # Abandoned test runner
mock                                # Superseded by unittest.mock
pep8                                # Renamed to pycodestyle
distribute                          # Merged back into setuptools

# =============================================================================
# EXTRAS / OPTIONAL DEPENDENCIES — [extra] syntax
# =============================================================================
requests[security]==2.19.0          # With security extras (pyOpenSSL, etc.)
celery[redis]==4.2.0                # With Redis transport
uvicorn[standard]==0.15.0           # CVE-2023-29003 — HTTP smuggling

# =============================================================================
# GIT / URL DEPENDENCIES — VCS and direct URL installs
# =============================================================================
# git+https://github.com/example/vulnerable-lib.git@v0.1.0#egg=vulnerable-lib
# git+https://github.com/example/old-fork.git@abc1234#egg=old-fork
# https://example.com/packages/legacy-package-1.0.tar.gz

# =============================================================================
# ENVIRONMENT MARKERS — platform/python version conditions
# =============================================================================
pywin32==227; sys_platform == "win32"           # Known vulnerable on Windows
subprocess32==3.5.4; python_version < "3.0"     # Backport for Python 2
typing==3.6.4; python_version < "3.5"           # Backport — unnecessary on 3.5+

# =============================================================================
# DEPRECATED / ABANDONED PACKAGES
# =============================================================================
pycrypto==2.6.1                     # CVE-2013-7459 — heap overflow, replaced by pycryptodome
python-crontab==2.2.0               # Known vulnerable version
fabric==1.14.0                      # Legacy version, Fabric2 is the maintained fork
South==1.0.2                        # Deprecated — Django native migrations replaced it
dj-database-url==0.5.0              # Unmaintained
django-extensions==2.0.0            # Known vulnerable version
django-debug-toolbar==1.9.0         # Known vulnerable version
django-cors-headers==2.1.0          # Known vulnerable version
djangorestframework==3.7.0          # Known vulnerable version
flask-cors==3.0.0                   # Known vulnerable version
flask-restful==0.3.6                # Known vulnerable version
flask-login==0.4.0                  # Known vulnerable version
